//Search Function will take a string to search for and an array of strings to search against
const searchInput = (input, attributes) => {};

//refreshes all cards using given array of data
const refreshCards = () => {};
